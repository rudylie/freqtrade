# Legacy Root Databases

SQLite databases moved from the old root-level `user_data/` layout on 2026-05-09.

Active bot databases should live under:

```text
user_data/bots/<environment>/<StrategyName>/data/
```
